import styled from "styled-components";

const Container = styled.div`
  display: flex;
  flex: 1;
  justify-content: center;
  align-items: center;
`;
const Wishlist = () => {
    return (
        <Container>
            <span>This is wishlist page...</span>
        </Container>
    );
};

export default Wishlist;
